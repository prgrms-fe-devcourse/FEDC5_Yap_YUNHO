import styled from "styled-components"

export const PostEditViewerLayout = styled.div`
  width: 46rem;
  min-width: 20rem;

  height: 63rem;

  background-color: ${({ theme }) => theme.colors.sub_alt};
  border-radius: ${({ theme }) => theme.radius.size12};
`
