import styled from "styled-components"

export const PostDetailViewerLayout = styled.div`
  width: 46rem;
  height: 63rem;

  background-color: ${({ theme }) => theme.colors.sub_alt};
  border-radius: ${({ theme }) => theme.radius.size12};
`
